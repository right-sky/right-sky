#include <stdio.h> 
#include <stdlib.h> 
int main() 
{ 
  char a[20] = { 'H','e','l','l','o',' ','W','o','r','l','d' }; 
  int i; 
  printf("加密前\n"); 
  for ( i = 0; i < 11; i++) 
  { 
    printf("%c", a[i]); 
  } 
  printf("\n"); 
  printf("加密后\n"); 
  for ( i = 0; i < 11; i++) 
  { 
    if (a[i] == 32) 
  { 
  printf("%c", a[i]); 
  } 
  if ((a[i]>=97)&&(a[i]<=122)) 
  { 
    if ((a[i]+5 >= 97) && (a[i]+5 <= 122)) 
    { 
    printf("%c", a[i] + 5); 
    } 
    else 
    { 
      printf("%c", a[i] + 5-26); 
    } 
  } 
  else if ((a[i] >= 65) && (a[i] <= 90)) 
  { 
    if ((a[i] + 5 >= 65) && (a[i] + 5 <= 90)) 
    { 
    printf("%c", a[i] + 5); 
    } 
    else 
    { 
    printf("%c", a[i] + 5 - 26); 
    }
    }
  }
  return 0; 
}
//you can know how to use the simple encryption in program C
//yep,and array!
